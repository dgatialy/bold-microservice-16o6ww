import { styled } from "../stitches.config";

export const Products = styled("div", {
  display: "flex",
  flexDirection: "column",
  gap: "$11",
  maxWidth: "1680px",
  margin: "0 auto",
  overflowX: "hidden"
});
